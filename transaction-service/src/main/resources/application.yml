spring:
  application:
    name: transaction-service
  profiles:
    active: ${ACTIVE_PROFILE}

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect